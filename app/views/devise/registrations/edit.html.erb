<div class="container">
  <div class="row">
    <h2>Welcome <%= @user.email %></h2>

    <div class="col-md-4">

      <img src="<%= asset_path 'votecompass2.jpg' %>" alt="" height="300px;">

    </div>

    <div class="col-md-8">
      <strong>What is my votecompass?</strong>
      <div class="">
        <br>Vote Compass is an online educational tool that promotes electoral literacy and democratic engagement. The tool allows users to receive information regarding the positions of political parties or candidates for office during a given election.
      </div>
      <div class="">
        <br>You can learn more or update your Votecompass at <a href="https://votecompass.abc.net.au/">https://votecompass.abc.net.au/</a>
      </div>
      <br>
      <%= render 'partials/editprofile' %>
    </div>
  </div>
  <div class="row ">
    <div class="col-md-12">
      <div class="col-md-4 profilebox">
        <table class="table">
          <thead>
            <tr>
              <th>Last 10 debates generated</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <!-- Note: Check out how to do a loop within the user controller for pulling the last 10 debates for that user -->
              <% @user.debates.each do |debate| %>
              <td><%= debate.topic %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <div class="col-md-4 profilebox scrollbox">
        <table class="table">
          <thead>
            <tr>
              <th>All of my debates</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <!-- Note: Check out how to do a loop within the user controller for pulling the last 10 debates they are following -->
              <% @user.debates.each do |debate| %>
              <% if debate.present? %>
              <td> <strong>Topic:</strong> <%= debate.topic %> <br> <strong>Description: </strong><%= debate.description %></td>
              <% else %>
              <td><%= puts "Come and join in some debates" %></td>
              <% end %>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      <div class="col-md-4 profilebox scrollbox">
        <!-- <h4>This will be a live feed of all recent submissions on debates I am apart of</h4> -->
        <table class="table">
          <thead>
            <tr>
              <th>My recent debate submissions</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <!-- Note: Check out how to do a loop within the user controller for pulling the last 10 debates the user has commented on -->
              <% @user.submissions.each do |submission| %>
              <td><%= submission.content  %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>

    </div>
  </div>
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
    </div>
  </div>
</div>
